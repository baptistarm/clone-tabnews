# clone-tabnews

Para configurar seu projeto:

- Execute o `npm install` para adicionar as dependências ao projeto.

Caso esteja utilizando `vsCode` como editor:

- É MANDATÓRIO instalar todas as extensões na sessão de "recomendados" para a padronização de código de todos os devs do projeto.
